# Optional frontend API override.
# Leave this at 8000 for the default FastAPI setup, or switch to 8001 if you start uvicorn on that port.
# VITE_API_BASE=http://localhost:8081

# Production frontend builds should point at the deployed backend.
VITE_API_BASE=https://stockqueryai-1-igzs.onrender.com
